# Place Lenny's Podcast transcript .txt or .md files here.
# Download from: https://github.com/lennyspodcast/transcripts
#
# Each file should be plain text. Optionally add a URL on the first line:
#   # URL: https://www.lennyspodcast.com/episode-name/
#
# After adding files, rebuild the index:
#   docker compose exec backend python -c "from app.knowledge.retriever import build_index; build_index(force=True)"
